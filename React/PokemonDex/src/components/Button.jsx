import React from "react";
import styled from "styled-components";

const Button = () => {
  return <DefaultButton>버튼</DefaultButton>;
};

export default Button;

const DefaultButton = styled.button``;
